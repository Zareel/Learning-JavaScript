/*
variable
datatype
primitive => single value
unmber, string, boolean, null, undefined

non-primitive => multiple value
array and onject

everything in javascript is an object
"use strict"

type of

template literal (template string)
js interpolation

let number = [1, 2, 3, 4, 5];
console.log(`Zareel ${number}`);

console.log(typeof null);

operators

*/

/*
Array
index and values (Fixed index)
object
key and value (key can change)
*/

let names = ["Zareel", "Shine", "Shon"];
